#!/bin/bash

SS_IMG="$(mktemp)"
mv "${SS_IMG}" "${SS_IMG}.png"
trap 'rm ${SS_IMG}.png' EXIT

grimshot save area "${SS_IMG}.png"
mogrify -resize 400% "${SS_IMG}.png"
tesseract "${SS_IMG}.png" "${1}" &> /dev/null

wl-copy < "${1}.txt"
exit
